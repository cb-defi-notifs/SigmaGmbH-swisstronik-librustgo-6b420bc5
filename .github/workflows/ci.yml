name: CI
on: [push]

jobs:
  build-shared-lib:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
      - name: Cache build artifacts
        uses: actions/cache@v3
        with:
          path: ~/.cache/sccache
          key: ${{ runner.os }}-sccache
      - name: Download sccache
        run: |
          wget https://github.com/mozilla/sccache/releases/download/v0.3.3/sccache-dist-v0.3.3-x86_64-unknown-linux-musl.tar.gz
          tar xf ./sccache-*.tar.gz
          ls -lah ./sccache*
          mv ./sccache*/sccache "$HOME/sccache"
      - name: Show version information
        run: |
          rustc --version; cargo --version; rustup --version
      - name: Create release build of librustgo
        run: |
          RUSTC_WRAPPER="$HOME/sccache" make build-rust
      - name: Upload shared library
        uses: actions/upload-artifact@v2
        with:
          name: Shared library
          path: ./internal/api/libwasmvm.x86_64.so
